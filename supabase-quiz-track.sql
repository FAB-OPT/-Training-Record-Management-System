-- ════════════════════════════════════════════════════════════════════
-- เพิ่มคอลัมน์ quiz_track ในตาราง quiz_meta
-- ใช้เก็บป้ายว่า "ข้อสอบข้อไหนใช้กับสายไหน" ของ Module ครัวที่แยกสาย
--   { "<uid ของคำถาม>": "steak" | "pasta" }
--   ข้อที่ไม่มีในนี้ = ใช้ได้ทั้งสองสาย
--
-- ต้องเก็บแยกแบบนี้เพราะข้อสอบชุดพื้นฐานอยู่ในโค้ดหน้าเว็บ แก้ที่ตัวข้อไม่ได้
--
-- วิธีใช้: เปิด Supabase → โปรเจกต์ cyjfgperenakjeazsfgf → SQL Editor
--          วางทั้งไฟล์นี้แล้วกด Run (รันซ้ำได้ ไม่พัง)
-- ════════════════════════════════════════════════════════════════════

alter table public.quiz_meta
  add column if not exists quiz_track jsonb not null default '{}'::jsonb;
